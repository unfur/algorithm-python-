> 吴帅超



### 一、选择（7*2=14）



1.标志着公钥密码学的出现的事件是（  B   ）

(A) Shannon 表《保密系统的通信理论》一文

(B) W.Diffie和M.Hellman 发表 《密码学的新方向》一文

(C) 希尔密码方案的提出

(D) RSA 加密方案的提出





2.以下行为属于主动攻击的是（  A？    ）

(A) 数据篡改及破坏

(B) 数据窃听

(C) 数据流分析

(D) 非法访间



3.DES 加密的选代轮次是（   B    ）

(A) 8

(B) 16

(C) 32

(D) 64



4.以下关于RSA 加密算法的说法中，错误的是（   D     ）

(A) RSA 算法是一种非对称加密算法

(B) RSA 算法可用于某种数字签名方案

（C）RSA 算法的安全性基于大整数分解难题

（D）RSA算法的运算速度比 DES 算法快



5.下列哪项是用来描述哈希函数（Hasn函数）的安全需求（    D？   ）

（A）不可区分性

（B）不可伪造性

（C）抗选择密文攻击

（D）抗碰撞性



6.以下关于数字签名说法正确的是（    	D？       ）

（A）数字签名是在传输的数据后加上一段和传输数据毫无关系的数字信息

（B）数字签名能够解决数据的加密传输，即安全传输问题

（C）数字签名一般采用对称加密机制

（D）数字签名能够解决篡改、伪造等安全性问题





7.如$a≡b\ (mod\ m)$，c 是任意整数，则(      A  ？      )

(A) $ac≡bc\ (mod\ m)$

(B) $a=b$

(C) $ac≠bc\ (mod\ m)$

(B) $a≠b$





### 二、填空（18分）

 

1.根据密码分析者破译时已具备的条件，把对密码系统的常见攻击分为唯密文攻击，-------，------，------四种常见的类型



（1）唯密文攻击；（2）已知明文攻击；

（3）选择明文攻击；（4）选择密文攻击。



2.AES加密包括四个部件。它们分别是字节代替、行移位、------、-----。

列混合、轮密钥加



3.数据加密标准（DES）是广泛使用和流行的一种分组密码算法，它的分组长度为——比特，密钥长度为——比特

64，64



4.ElGamal密码体制的困难下是基于——。

离散对数问题



5.整数 22008 和 225 的最大公因数$gcd（22008，225）$=   ——





### 三、判断（8*1.5=12）

1. 密码学是研究信息系统安全保密的科学，它包括密码编码学和密码分析学。（        ）
2. P盒变换是指把一个比特串各比特的位置次序重新排列得到一个新的比特串，能起到混乱的效果。（              ）
3. IDEA密码算法采用混合乘加的特殊网络结构来实现混淆和扩散。（          ）
4. 分组密码算法AES 的数据密钥长度为 64 比特。（            ）
5. 电子密码本模式（ECB）的缺点是相同的明文分组产生不同的密文分组。（           ）
6. 公钥加密体制是一种加密密钥与解密密钥不同的密码体制。（              ）
7. 如果群G上的DDH问题是困难的，那么在这个群G上的ElGamal加密方案是IDA-CPA安全的。（              ）
8. 设φ是欧拉函数，若任意两个整数m和n满足$gcd(m,n)=1$，则$φ(mn)=φ(m-1)φ(n-1)$。（              ）





### 四、简答题（5题，28分）



1.请简述当密码算法满足哪两个准则的其中之一时，密码算法在实际中是可用的？并给出一个无条件安全的加密方案（5分）



**（1） 破译密文的代价超过被加密信息的价值。**

**（2 )  破译密文所花的时间超过信息的有用期。**



**一次一密方案(一次一密乱码本)**





2.请简述什么是分组密码？分组密码构造应该遵循的准则（至少3条）？并解释为什么非对称密码的出现没有完全代替对称密码？（7分）



**若明文流被分割成等长串，各串用相同的加密算法和相同的密钥进行加密，就是分组密码。**



**准则：**

- **要有足够大分组长度（保证足够大的明文空间，避免给攻击者提供太多的明文统计特征信息）**

- **密钥空间要尽可能大（防止穷举密钥）**

- **保证足够强的密码算法复杂度以加强分组密码算法自身的安全性，**

  **方法如下：**

  - **先将一个明文分组划分为若干子组分别进行处理，然后合并起来再做一些适当的变换，以增大密码算法强度。采取这样的措施也便于密码算法的实际分析和评测**

  - **采用乘积密码的思想。通过两种或两种以上简单密码的逐次应用，构成强度比其中任何一个更强的加密结果。有效克服单一密码变换的弱点**

- **软件实现尽量采用子块和简单运算，采用加法、乘法、异或和移位等指令，易于标准处理器完成运算**
- **加解密硬件结构最好一致，这样便于应用超大规模集成芯片实现。以简化系统整体结构的复杂性。**



**对称密码拥有非对称密码不可比拟的一个优点是：快。**







3.请简述什么是陷门单向函数？什么密码方案可以用陷门单向函数构造？（4分）



**陷门单向函数是这样的单向函数，存在一个附加信息，当不知道该附加信息时，从函数值求原像是困难的，但当知道该附加信息时，从函数值求原像就变得容易了。**

> **即陷门单向函数在附加信息未知时是单向函数，而当附加信息已知时，就不再是单向函数了。通常把附加信息称为陷门信息。**



**公钥密码方案可以用陷门单向函数构造**







4.在密码分组链接模式（CBC）中，当前的明文分组 $M_i$ 要先与前一个密文分组 $C_{i-1}$ 进行异或再进行加密，公式为：$C_i=Enc_k(M_i\ ⊕C_{i-1})$。下图为CBC模式对消息$m=m[0]||m[1]||m[2]||m[3]$ 进行加密的示意图，请根据该加密示意图画出对应的解密示意图（注：$E(k,·)$和$D(k,·)$分别表示该分组密码的加密和解密算法）（5分）

![image-20231204210611050](https://wiper3.oss-cn-guangzhou.aliyuncs.com/blogimage/202312042106096.png)













5.在RSA 假设下，RSA 加密方案在 OW-CPA 安全模型下是安全的，但是在IND-CPA 安全模型下是不安全的。请写出 OW-CPA 安全模型的形式化定义，并给出 RSA加密方终在IND-CPA 安全模型下不安全的原因， (7 分)



























### 五、分析解答题（5题，28分）



1.仿射加密是单表加密的一种，其加密算法 E(m)和解密算法 D(c)如下所示

- E(m)：输入明文$m ∈ [0,25]$，计算并输出密文$c=(a*m+b)\mod26$。
- D(c)：给定密文$c ∈ [0,25]$,计算明文$m=a^{-1}(c-b)\mod26$。,其中 $aa' ≡ 1 \mod 26$。

假设a =3，b=11，请计算：

1）明文 $m=WORLD$ 对应的密文。(2 分)

2）密文$c=RTE $对应的明文。(4 分)



















2.在RSA加密方案中，公钥$(n,e)=(33,3)$，其中$n =pq，p=3，q=11$，请计算该RSA 方案的私钥d，假设明文m =10，请写出加密、解密的过程。(6分)















3.EIGamal 签名方案由密钥生成算法$KeyGen(k)$、签名算法$Sign(sk,m)$ 和验证算法$Verify(pk,m,σ)$ 所组成，其中密钥生成算法和验证算法如下所示，请据此推导出签名算法。 (6 分)

- 密钥生成算法$KeyGen(k)$：输入安全参数k，设p是一个大素数，使得模 p 上的离散对数问题是困难的，G为阶是p的群，选取生成元g ∈ G，此外选取一个防碰撞的 Hash 函效$H:\{0,1\}^* → Z_p^*$，随机选取$x ∈ Z_p^*$，令$y = g^x$，公钥为$pk =(G,p,g,y,H)$，私钥$sk =x$。
- 验证算法$Verify(pk,m,σ)$：检查$g^{H(m)} =y^rr^s$是否成立，若成立，输出 1；否则输出 0

$y^rr^s≡(g^x)^r(g^k)^s≡$



签名算法$Sign(sk,m)$：

选取随机数 $0<k<p-1$ 且 $gcd(k,p-1)=1$，计算$r≡g^k(\mod p)$ 和 $s≡k^{-1}(H(m)-xr)(\mod p-1)$，最后输出签名 $σ=(r,s)$























4.DSS 签名方案由密钥生成算法$KeyGen(k)$、签名算法$Sign(sk,m)$ 和验证算法$Verify(pk,m,σ)$ 所组成。请根据以下给出的密钥生成算法及部分的签名算法、验证算法，将签名算法、验证算法内容补充完整。 (6分)

- 密钥生成算法$KeyGen(λ)$：输入安全参数λ， 该算法首先远取一个 λ 比特的大素数 $p\ (512 ≤ λ ≤ 1024)$，且(p -1)有一个160 比特的素因子q，然后选取一个模 p 的阶为 q 的数 g。接着随机选取 $x ∈ Z_q^*$，计算$y = g^x$。此外，还选取一个防碰撞的 Hash 函效$H:\{0,1\}^* → Z_q^*$，最后输出公钥为$PK =(G,p,g,y,H)$，私钥$sk =x$。
- 签名算法$Sign(sk,m)$：输入私钥 $sk =x$ 和消息 $m∈\{0,1\}^*$，该算法随机选取 $k∈Z_q^*$，计算 $r = (g^k\mod p)\mod q$，计算$s =\_\_\_\_\_\_\_\_\_$，最后输出签名$σ=(r,s)$
- 验证算法$Verify(pk,m,σ)$为：输入公钥$PK =(G,p,g,y,H)$，消息 m 和签名$σ=(r,s)$，该算法首先计算 $w=s ^ {-1} \mod q$，$u1=H(m)w \mod q$，$u_2 =\_\_\_\_\_\_\_\_\_$，然后验证等式$\_\_\_\_\_\_\_\_\_$是否成立，若等式成立，则签名有效，否则，数字签名无效。



$s=(k ^ {-1} (H(m)+xr)) \mod q$



$u_2=rw \mod q$

$(g ^ {u_1} y ^ {u_2} \mod p) \mod q=r$























5.EIGamal 公钥密码体制由 EIGamal 在 1985 年提出，其方案由密钥生成算法$KeyGen(k)$、加密算法$Enc(pk,m)$ 和解密算法$Dec(sk,C)$ 所组成。其中密钥生成算法和解密算法如下所示。请据此推导出加密算法。(4 分)

- 密钥生成算法$KeyGen(k)$： 输入安全参数 k ， 选取 k 比特大素数 p 阶群 G，选取生成元$g∈G$，及$x ∈ Z_p^*$，计算$y = g^x$，公钥为$pk =(G,p,g,y)$，私钥$sk =x$。
- 解密算法$Dec(sk,C)$ ：输入私钥 sk 和，密文 C，计算$m= C_2/C_1^x$



